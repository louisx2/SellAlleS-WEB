# Supabase (se usará en la siguiente fase)
# Encuentra estos valores en el panel de Supabase -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_anon_key

# Nota: por ahora la app arranca con datos de demostración en memoria,
# así que no necesitas estas variables para correr `npm run dev`.
